Code Style: Minimal & Concise
コードの冗長性を徹底的に排除し、以下のルールを厳守せよ。

No Redundancy: 「丁寧さ」は不要。最短・最小のコードで記述。

No Over-Handling: 例外処理（try-except）は最小限に。正常系のみで良い。

No Extra Functions: テスト用関数や if __name__ == "__main__": 等は一切不要。

Minimal Output: print() は結果表示に必須なもの以外禁止。

Code Only: 解説は省き、コード本体のみを提示。